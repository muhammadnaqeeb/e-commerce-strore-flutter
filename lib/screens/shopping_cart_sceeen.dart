import 'package:flutter/material.dart';

class ShoppingCarTScreen extends StatelessWidget {
  const ShoppingCarTScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Shoping Card'),
      ),
    );
  }
}
